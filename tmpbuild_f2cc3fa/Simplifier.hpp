#ifndef SIMPLIFIER_HPP#define SIMPLIFIER_HPP#include "math.hpp"#include <map>#include <optional>#include <queue>#include <vector>struct CollapseCandidate {    Vertex* a = nullptr;    Vertex* b = nullptr;    Vertex* c = nullptr;    Vertex* d = nullptr;    APSCCollapseResult collapse{};    std::size_t a_version = 0;    std::size_t b_version = 0;    std::size_t c_version = 0;    std::size_t d_version = 0;};struct CollapseCandidateCompare {    bool operator()(const CollapseCandidate& lhs, const CollapseCandidate& rhs) const;};using CollapseQueue =    std::priority_queue<CollapseCandidate, std::vector<CollapseCandidate>, CollapseCandidateCompare>;bool CanFormCandidate(const Vertex* a, const Vertex* b, const Vertex* c, const Vertex* d);std::optional<CollapseCandidate> BuildCandidate(    Vertex* a,    Vertex* b,    Vertex* c,    Vertex* d);bool IsCandidateStillValid(const CollapseCandidate& candidate);void PushCandidateIfPossible(    CollapseQueue& queue,    Vertex* a,    Vertex* b,    Vertex* c,    Vertex* d);void QueueLocalCandidatesAround(CollapseQueue& queue, Vertex* center);CollapseQueue BuildInitialCollapseQueue(const std::map<int, Vertex*>& ring_heads);std::optional<CollapseCandidate> PopBestValidCandidate(CollapseQueue& queue);#endif